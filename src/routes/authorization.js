const authorizationComponent = () => {
    const authoriationRoute = "/authorization";
    const authorizationCallback = (req, res) => {
        console.log("i'm in");
    }
}

export default authorizationComponent;